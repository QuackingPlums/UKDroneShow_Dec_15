Copy the scripts, snips and empty results folder to the github project page.
Edit the top.snip and bottom.snip HTML as appropriate for the event.
After each race, copy the HTML output from Laps to the results folder - rename the file if necessary.
Run _update.sh and push the changes to github.

After the event, tidy up the results where required.